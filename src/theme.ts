// my-theme.ts
import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  textColor: '#f5f6fa',
  bgColor: '#2f3640',
  accentColor: '#e1b12c',
  borderRadius: `14px`,
};

export { theme };
